# 3.3-asmr — ASMR & Oddly Satisfying

**Engine**: ai-video on Veo 3.1 + E5 native audio (no TTS, no music)
**Storyboard**: `examples/storyboards/3.3-asmr.json`

## LLM Evaluation Target

Audio-visual sync: linking each micro-action (tap, pour, brush) to its corresponding audio spike.

## Expected detections (grading checklist)

- [ ] Analysis identifies the video as ASMR/sensory content
- [ ] Specific micro-sounds attributed to specific on-screen actions
- [ ] No speech transcript expected - audio is ambient/foley only

## Notes

Blocked on E5 (Veo native-audio passthrough). Single Veo exception to the MiniMax batch (~$16).

## Results

- **mp4**: none generated
- **analyze post_id**: none
- **Rubric score** (docs/quality-rubric.md, /35): N/A
- **Eval target**: BLOCKED
- **Analysis excerpt**:

```
(no generation attempted end-to-end — see below)
```

E5 (Veo native-audio passthrough) was built and verified: `generate_audio` correctly omitted for Developer-API-mode auth, `duration_seconds` correctly quantized to Veo's actual accepted set `{4, 6, 8}` (its own error text describes a continuous "4 to 8, inclusive" range, but live testing showed 5 and 7 are rejected too). Generation itself is blocked purely at the account level: the supplied Google API key has zero Veo generation quota/billing enabled — every attempt returns `429 RESOURCE_EXHAUSTED` regardless of duration or prompt. This is not a code defect; it requires the account owner to enable billing/quota on the backing GCP project. Reported to scrollmark/showrunner#63 as an explicit blocker rather than worked around.

## Iteration log

Paused pending Veo quota/billing being enabled on the project backing the supplied API key. No further code changes needed to unblock — once quota is available, `showrunner create --storyboard examples/storyboards/3.3-asmr.json --format ai-video ...` (with `providers.video: gemini` in `.showrunner.yaml`) should complete without further debugging.
