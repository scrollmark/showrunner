# 2.1-greenscreen — Greenscreen / Reaction Commentary

**Engine**: composite (E4) - speaker overlay on article background
**Storyboard**: `examples/storyboards/2.1-greenscreen.json`

## LLM Evaluation Target

Foreground/background segmentation: separating the speaker cutout from the content being discussed; tracking gestures toward it.

## Expected detections (grading checklist)

- [ ] Analysis distinguishes the speaker layer from the background document
- [ ] Background content (article/image) described independently
- [ ] Speaker's commentary linked to the background subject

## Notes

Blocked on E4 (compositing).

## Results

- **mp4**: `examples/output/2.1-greenscreen.mp4`
- **analyze post_id**: `ba119d58-d4ff-4896-a1b9-ed3ad5beaa79`
- **Rubric score** (docs/quality-rubric.md, /35): N/A — composite/photorealistic output, rubric reserved for 2.2 (see 1.1's card for the general note).
- **Eval target**: PARTIAL
- **Analysis excerpt**:

```
Hook
  - Uses a common and controversial self-improvement topic to grab attention.

Scenes (2)
  1. [0.0] A woman reads a blog post claiming early wake-ups lead to success.
  2. [4.3] She expresses shock at the 5 AM time and decides to prioritize sleep instead.
```

Checklist: item 3 (speaker's commentary linked to the background subject) ✅ — "reads a blog post... expresses shock" ties her reaction directly to the article's content. But items 1 and 2 (explicitly distinguishing the speaker cutout as a separate visual layer from the background document, describing the background independently) aren't evidenced — the report renders this as one unified scene rather than calling out a foreground/background composite structure at all. It's not clear whether that's because the model didn't perceive the layering, or because the default `--report` narrative format collapses everything into plot beats regardless. Downgrading from an earlier "pass" call to PARTIAL on rereading — correctly inferring the *content* of both layers isn't the same as *detecting the compositing itself*, which is the actual eval target here.

## Iteration log

(refine/regen actions taken, if any)
